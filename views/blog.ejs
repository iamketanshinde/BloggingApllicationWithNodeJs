<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>BlogRernderImage</title>
    <%- include('./partials/head.ejs') %>
</head>
<body>
    

    <div class="container mt-4 mb-4">
        <h1><%= blog.title %></h1>
        <img src="<%= blog.coverImgUrl %>" alt="Blog Image" width="200px">
        <pre><%= blog.body %></pre>
    </div>

    <div class="container mt-3 d-flex">
        <img src="<%= blog.createdBy.profileImageUrl %> " alt="" width="50px">
        <p><%= blog.createdBy.fullname %></p>
    </div>

    <div class="container mt-3">
        <h1>Comments</h1>
        <% if (locals.user) { %>
            <form action="/blog/comment/<%=blog._id %>" method="post">
                <div class="mb-3">
                  <input type="text" name="content" class="form-control" placeholder="Enter Your Comments!!">
                  <button class="btn btn-sm btn-info mt-2">SubmitComment</button>
                </div>
            </form> 
        <% } %>
    </div>


    <%- include('./partials/scripts.ejs') %>
</body>
</html>
